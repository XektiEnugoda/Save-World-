#pragma once
#include <SFML/Graphics.hpp>

class mMenu()
{
  public:
    mMenu();
    Title();
    Play();
    Options();
};


/*enum MainMenu
{
  public:
    MainMenu();
    Title();
    PlayButton();
    StartGame();
};*/
